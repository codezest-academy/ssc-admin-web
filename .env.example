# In local development, leave this as /api/v1 because vite.config.ts proxies requests to avoid CORS.
# In production, set this to the full URL (e.g., https://api.codezest.in/api/v1)
VITE_API_URL=/api/v1
VITE_API_TIMEOUT=10000
